# FINAL SOLUTION - Copy this into Plesk "Deploy actions" field
# This explicitly uses Node.js 24.9.0 from nodenv

# Set Node.js 24.9.0 path
export PATH="$HOME/.nodenv/versions/24.9.0/bin:$PATH"

# Backend deployment
cd backend
npm ci --production
pm2 restart mooncode-backend || pm2 start src/index.js --name mooncode-backend

# Frontend deployment
cd ../frontend
npm ci
npm run build

echo "✅ Deployment complete at $(date)"
