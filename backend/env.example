# ============================================
# BuySial Commerce Backend - Environment Configuration
# ============================================

# Database Configuration
# ----------------------
# MongoDB Connection String (Required for persistent storage)
# Option 1: MongoDB Atlas (Recommended - Free tier available)
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/buysial?retryWrites=true&w=majority

# Option 2: Local MongoDB
# MONGO_URI=mongodb://localhost:27017/buysial

# Option 3: Docker MongoDB with auth
# MONGO_URI=mongodb://admin:password@localhost:27017/buysial?authSource=admin

# Explicit database name (optional - can be in connection string)
DB_NAME=buysial

# Disable in-memory database (IMPORTANT - set to false for production)
USE_MEMORY_DB=false

# Server Configuration
# --------------------
PORT=3000
NODE_ENV=production

# CORS Configuration
# Allow multiple origins separated by comma, or use * for all
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,*

# Security
# --------
# JWT Secret for authentication (Generate a random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-to-random-string

# Google Maps API (Optional - fallback if not set in database)
# -------------------------------------------------------------
# Get API key from: https://console.cloud.google.com/google/maps-apis
# Enable: Geocoding API, Maps JavaScript API
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Gemini AI API (Optional - fallback if not set in database)
# -----------------------------------------------------------
# Get API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Facebook API (Optional)
# -----------------------
FACEBOOK_ACCESS_TOKEN=your-facebook-access-token
FACEBOOK_APP_ID=your-facebook-app-id

# WhatsApp Integration (Optional)
# --------------------------------
ENABLE_WA=true

# Moyasar Payment Gateway (Saudi Arabia - Mada & Apple Pay)
# ----------------------------------------------------------
# Get API keys from: https://dashboard.moyasar.com
# Required for accepting Mada debit cards and Apple Pay
MOYASAR_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxx
MOYASAR_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxx
MOYASAR_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxx

# Apple Pay Domain (your verified domain for Apple Pay)
APPLE_PAY_DOMAIN=yourdomain.com
STORE_NAME=BuySial Store

# Frontend URL for payment callbacks
FRONTEND_URL=https://yourdomain.com

# File Upload Limits
# ------------------
MAX_FILE_SIZE=5mb

# Logging
# -------
LOG_LEVEL=info

# ============================================
# DEPLOYMENT INSTRUCTIONS:
# ============================================
# 1. Copy this file to .env
# 2. Replace all placeholder values with real credentials
# 3. NEVER commit .env to Git (it's in .gitignore)
# 4. For production, set USE_MEMORY_DB=false and provide MONGO_URI
# 5. Generate strong JWT_SECRET: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
